version: 2

jobs:
  # test:
  #   docker:
  #     - image: circleci/node:10
  #   steps:
  #     - checkout
  #     - run:
  #         name: Install project
  #         command: npm install
      #- run:
      #    name: Test project
      #    command: npm test

  docs-build-deploy:
    docker:
      - image: circleci/node:10
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Install project
          command: npm install
      - run:
          name: Build project
          command: npm run build-storybook
      - add_ssh_keys:
          fingerprints:
            - "47:ea:88:91:2e:76:b5:8e:26:06:76:3d:d6:f7:9f:f7"
      - run:
          name: Just random git stuff
          command: |
            git config user.email "ci-build@randomemail.co.za"
            git config user.name "ci-build"
      - run:
          name: deploy project
          command: npm run gh-pages --message "ci deployment" --dist storybook-static

workflows:
  version: 2
  build:
    jobs:
      - docs-build-deploy

#for when it's ready
# workflows:
#   version: 2
#   build:
#     jobs:
#       - docs-build
#       - docs-deploy:
#           requires:
#             - test
#             - docs-build
#           filters:
#             branches:
# only: master
